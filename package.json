{
  "name": "spa-login",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "coverage": "istanbul cover _mocha -- --no-cache --recursive \"test/**/*.js\"",
    "test": "mocha --no-cache --recursive \"test/**/*.js\"",
    "start-dev": "nodemon ./bin/www",
    "start": "node ./bin/www"
  },
  "dependencies": {
    "body-parser": "~1.15.2",
    "cookie-parser": "~1.4.3",
    "debug": "~2.2.0",
    "express": "~4.14.0",
    "morgan": "~1.7.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "nodemon": "^1.11.0",
    "supertest": "^2.0.1"
  }
}
